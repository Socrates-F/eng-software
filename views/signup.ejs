<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
            crossorigin="anonymous"
        />
        <link rel="stylesheet" href="/css/styles.css" />
        <title>Signup</title>
    </head>
    <body
        class="p-5 d-flex flex-column align-items-center justify-content-center"
    >
        <nav class="navbar fixed-top">
            <ul class="nav">
                <li class="nav-link" href="/">Home</li>
                <li class="nav-link" href="/login">Login</li>
            </ul>
        </nav>

        <form
            id="register"
            method="post"
            action="/register"
            class="d-flex flex-column"
        >
            <img src="/public/images/financedu.png" alt="logo da empresa" class="img-fluid mb-3" />

            <!-- Email input -->
            <div class="form-outline mb-4">
                <input
                    type="text"
                    name="fName"
                    class="form-control"
                    placeholder="Ex: John Doe"
                    pattern="[^\d]{5,}"
                    title="O nome deve ter no mínimo 5 caracteres e não pode conter números"
                    required
                />
                <label class="form-label" id="full-name" for="fName"
                    >Full name</label
                >
            </div>

            <div class="form-outline mb-4">
                <input
                    type="email"
                    name="email"
                    class="form-control"
                    placeholder="Ex: address@email.com"
                    pattern=".{5,}"
                    title="O email deve ter no mínimo 5 caracteres"
                    required
                />
                <label class="form-label" for="email">Email address</label>
            </div>

            <!-- Password input -->
            <div class="form-outline mb-4">
                <input
                    type="password"
                    class="form-control"
                    placeholder="Ex: tc38pfj"
                    name="pass"
                    required
                />
                <label class="form-label" for="pass">Password</label>
            </div>
            <!-- title="A senha deve ter no mínimo 6 caracteres" pattern=".{6,}"
        <div class="form-outline mb-4">
            <input type="password" class="form-control" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{6,}" title="A senha deve ter no mínimo 6 caracteres, pelo menos uma letra maiúscula, uma letra minúscula e um número" required/>
            <label class="form-label" for="form2Example2">Confirm password</label>
        </div> -->

            <!-- Submit button -->
            <button
                type="submit"
                class="btn btn-primary align-self-center btn-block mb-4"
            >
                Sign up
            </button>
        </form>

        <% if (locals.success) { %>
        <script>
            alert("Usuário criado com sucesso");
        </script>
        <% } else {%>
        <script>
            alert("Erro ao criar usuário");
        </script>
        <% }%>

        <script
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"
        ></script>
        <script src="/js/front.js"></script>
    </body>
</html>
